import React from "react";

function Vmc() {
    return(
        <section className="section bg-c-light border-top">
                <div className="container">
                    <div className="row">
                        <div className="col-md-12 mb-5 text-center">
                            <h3 className="main-heading">Vision Mission and Values</h3>
                            <div className="underline mx-auto"></div>
                        </div>
                        <div className="col-md-4 text-center">
                            <h6>Our Vision</h6>
                            <p>
                                The carousel is a slideshow for cycling through a series of content, built with CSS 3D transforms and a bit of and a bit of JavaScript. It works with a series of images, text, or custom markup. It also includes  is not visible to the user (such as when the browser tab is inactive, the browser window is minimized, etc.).
                            </p>
                        </div>
                        <div className="col-md-4 text-center">
                            <h6>Our Mission</h6>
                            <p>
                                The carousel is a slideshow for cycling through a series of content, built with CSS 3D transforms and a bit of and a bit of JavaScript. It works with a series of images, text, or custom markup. It also includes  is not visible to the user (such as when the browser tab is inactive, the browser window is minimized, etc.).
                            </p>
                        </div>
                        <div className="col-md-4 text-center">
                            <h6>Our Core Values</h6>
                            <p>
                                The carousel is a slideshow for cycling through a series of content, built with CSS 3D transforms and a bit of and a bit of JavaScript. It works with a series of images, text, or custom markup. It also includes  is not visible to the user (such as when the browser tab is inactive, the browser window is minimized, etc.).
                            </p>
                        </div>
                    </div>
                </div>
            </section>
    );
}

export default Vmc;